#pragma once



class StateMachine {
  public:
  //constructor
  StateMachine(std::string states);

  //StateMachine member methods
  void change(std::string states, parameters???);
  void update();
  void render();